{
  "name": "organvm-portfolio",
  "type": "module",
  "version": "1.0.0",
  "description": "ORGANVM — Eight-Organ Creative-Institutional System Portfolio",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "python3 ../scripts/praxis-portfolio-generate.py --output-dir src/data && cp ../system-metrics.json src/data/",
    "build": "astro build",
    "preview": "astro preview",
    "generate-data": "python3 ../scripts/praxis-portfolio-generate.py --output-dir src/data && cp ../system-metrics.json src/data/"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "@astrojs/sitemap": "^3.0.0"
  },
  "devDependencies": {
    "d3": "^7.9.0"
  }
}
